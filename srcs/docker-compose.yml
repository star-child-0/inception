version: '3'

services:
  nginx:
    container_name: nginx
    build:
      context: requirements/nginx/
      dockerfile: Dockerfile
    ports:
      - 443:443
    networks:
      - inception
    restart: always

networks:
    inception:
        driver: bridge
